on_actions = {
	on_startup = {
		effect = {
			RHE = {
				every_state = { limit = { is_core_of = prev } remove_core_of = prev }
			}
			KPD = {
				every_state = { limit = { is_core_of = prev } remove_core_of = prev }
			}
			NSD = {
				every_state = { limit = { is_core_of = prev } remove_core_of = prev }
			}
			DBR = {
				every_state = { limit = { is_core_of = prev } remove_core_of = prev }
			}
		}
	}
}